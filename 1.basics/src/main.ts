let username = 'gdpp';

console.log(username);

let a: number = 20;
let b: number = 6;
let c: number = 2;

console.log(a / b);
console.log(c * b);

let myName: string;
myName = 'Gustavo';

let meaningOfLife: number;
meaningOfLife = 2;

let isLoading: boolean;
isLoading = true;

let album: any;
album = 'Fear Inoculum';

let union: string | number;
union = 3;

let postId: string | number;
let isActive: boolean | number;

let re: RegExp = /\w+/g;

let strArr = ['one', 'punch', 'man'];
let guitars = ['Strat', 'Les Paul', 5150];
let mixData = ['EVH', 1984, true];
